{
  "name": "messenger",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run clear && parcel static/index.html -p 3000 --no-cache",
    "build": "npm run clear && parcel build static/index.html",
    "server": "node server",
    "clear": "rimraf dist .cache"
  },
  "engines": {
    "node": ">=12.x"
  },
  "devDependencies": {
    "@inventory/parcel-plugin-handlebars": "^1.0.0",
    "express": "^4.17.1",
    "parcel-plugin-handlebars-precompile": "^1.0.2"
  },
  "dependencies": {
    "handlebars": "^4.7.7",
    "parcel": "^2.0.0",
    "parcel-bundler": "^1.12.5",
    "postcss": "^8.3.6",
    "rimraf": "^3.0.2",
    "sass": "^1.38.0"
  },
  "staticFiles": {
    "staticPath": "static"
  }
}
